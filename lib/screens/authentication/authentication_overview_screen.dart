import 'package:flutter/widgets.dart';

class AuthenticationOverviewScreen extends StatelessWidget {
  const AuthenticationOverviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
